<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | <%= settings.site_name || 'Logistic Company' %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: <%=settings.primary_color || '#1a73e8' %>;
        }
    </style>
</head>

<body>
    <header>
        <div class="container">
            <nav>
                <div class="logo">
                    <i class="fas fa-truck-fast"></i>
                    <%= settings.site_name || 'LogisticCo' %>
                </div>
                <div class="menu-toggle" onclick="document.querySelector('.nav-links').classList.toggle('active')">
                    <i class="fas fa-bars"></i>
                </div>
                <ul class="nav-links">
                    <li><a href="/" class="<%= path === '/' ? 'active' : '' %>">Home</a></li>
                    <li><a href="/services" class="<%= path === '/services' ? 'active' : '' %>">Services</a></li>
                    <li><a href="/tracking" class="<%= path === '/tracking' ? 'active' : '' %>">Track</a></li>
                    <li><a href="/about" class="<%= path === '/about' ? 'active' : '' %>">About</a></li>
                    <li><a href="/contact" class="<%= path === '/contact' ? 'active' : '' %>">Contact</a></li>
                    <% if (user) { %>
                        <% if (user.role==='admin' ) { %>
                            <li><a href="/admin/dashboard" class="btn">Admin Panel</a></li>
                            <% } else { %>
                                <li><a href="#" class="btn" onclick="return false;">Hi, <%= user.username %></a></li>
                                <% } %>
                                    <li><a href="/logout">Logout</a></li>
                                    <% } else { %>
                                        <li><a href="/login">Login</a></li>
                                        <li><a href="/quote" class="btn">Get Quote</a></li>
                                        <% } %>
                </ul>
            </nav>
        </div>
    </header>
    <main>